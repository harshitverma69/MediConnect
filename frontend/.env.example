# Copy to .env in this folder. Restart Vite after changes.

# Backend API — must be your Node/Express base URL only (no /api suffix, no trailing slash).
# On Render you usually have TWO URLs: (1) Static Site = patient UI, (2) Web Service = Express API.
# VITE_BACKEND_URL must be (2), NOT (1). Test (2) in browser: .../api/doctor/list → JSON, not HTML.
# AI routes: /api/ai/* (Express needs AI_SERVICE_URL for the Python service).
VITE_BACKEND_URL=http://localhost:4000

# Razorpay Checkout — use the same Key ID as backend RAZORPAY_KEY_ID (Test/Live mode must match)
VITE_RAZORPAY_KEY_ID=
